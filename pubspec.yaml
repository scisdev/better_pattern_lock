name: better_pattern_lock
description: A better pattern lock for Flutter.
version: 1.0.0
homepage: https://github.com/scisdev/better_pattern_lock

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true